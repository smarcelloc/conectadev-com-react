import { makeStyles } from "@material-ui/styles";

const useStyle = makeStyles((theme) => ({


}));


function Feed() {
  const classes = useStyle();

  return (
    <div>Feed</div>
  );
}

export default Feed;